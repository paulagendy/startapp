<!DOCTYPE html>
<html>
  <head>
    <title>Startapp</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>
    <%= yield %>
  </body>
</html>
<style>
  @font-face {
    font-family: "mona";
    src: url("https://public-asse.s3.ca-central-1.amazonaws.com/fonts/Mona-Sans.ttf");
    font-weight: 200 900;
  }
  body {
    background-color: var(--bg-color);
    font-family: mona;
  }
  :root {
    --bg-color: #FFFCF4;
    --primary-container: #D8ED7C;
    --on-primary: #181E00;
    --secondary-container: #404C00;
    --on-secondary:  #D8ED7C;
    --stroke: 1px solid #77786A;
    --txt-primary: hsla(72, 100%, 6%, 0.87);
    --txt-secondary: hsla(72, 100%, 6%, 0.7);
    --surface-z2: linear-gradient(0deg, rgba(85, 101, 0, 0.08), rgba(85, 101, 0, 0.08)), #FFFCF4;
  }
  body {
    accent-color: #D8ED7C !important;
  }
  h1, h2 {
    color: var(--txt-primary);
    font-family: mona;
  }
  .card {
    background: var(--surface-z2);
    border-radius: 8px;
    border: var(--stroke);
    overflow: clip;
    padding: 8px;
    padding-bottom: 24px;
    margin-bottom: 12px;
  }
  button {
    padding: 12px 16px;
    border-radius: 4px;
    font-weight: 500;
    border: 1px solid var(--on-primary);
  }
  input[type="submit"] {
    width: 100%;
    padding: 12px 16px;
    border-radius: 4px;
    background-color: var(--primary-container);
    color: var(--on-primary);
    border: 1px solid var(--on-primary);
    font-weight: 500;
  }
  .form-control:focus {
    border: 1px solid hsla(71, 76%, 71%, 1);
    box-shadow: 0 0 0 0.25rem hsla(71, 76%, 71%, 0.25);
  }
  .avatar-img {
    width: 120px;
    height: 120px;
    border-radius: 100vw;
    border: var(--stroke);
    margin-top: 20px;
    object-fit: cover;
  }
    input[type="submit"] {
    width: 100%;
    padding: 12px 16px;
    border-radius: 4px;
    background-color: var(--primary-container);
    color: var(--on-primary);
    border: 1px solid var(--on-primary);
    font-weight: 500;
  }
</style>
